This directory contians *.json files for use with the `--test json` Fulcrum CLI
option (requires that Fulcrum be compiled with -DENABLE_TESTS).

It also is included with the "Json" stand-alone lib, used by the `test` CLI arg
to the test binary that is produced if one compiles that project.

